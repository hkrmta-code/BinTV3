BIN TV iOS — IPA BUILD FIX (2026-09-12) — phan tich tu xbuild.log that
========================================================================
Run duoc phan tich: https://github.com/Binkyo2013/BinTV1/actions/runs/34690803983
Loi: Preflight FAIL — "3 file .swift/.m KHONG nam trong Compile Sources":
     BinTV/Views/BrowserTabBar.swift, BrowserTabs.swift, NewTabPageView.swift
Nguyen nhan: repo da co pbxproj build 219 (dung) nhung 3 file khai tu cua
build 218 VAN CON tren repo (zip khong bieu dien duoc thao tac xoa).

CACH AP DUNG (100% upload, KHONG can xoa file thu cong):
1. UPLOAD/GHI DE 9 file trong zip nay len repo (giu nguyen duong dan):
   - BinTV/Views/BrowserTabs.swift        <- TOMBSTONE (chi comment + '// ci-skip:')
   - BinTV/Views/BrowserTabBar.swift      <- TOMBSTONE
   - BinTV/Views/NewTabPageView.swift     <- TOMBSTONE
     (3 file nay DE LEN file cu — code chet bien mat, preflight bo qua CO
      CHU DICH va in minh bach "bo qua co chu dich" ra xbuild.log)
   - .github/workflows/build-ipa.yml      <- fix 'set +e' (marker [STEP END]
     se duoc ghi ca khi step fail; RC that van giu nguyen)
   - tests/mock-e2e/test_project_consistency.py + README.md (326 assertion)
   - BAOCAO-KYTHUAT-FIX-2026-09-12.md (Addendum 6), README.md
2. Push/commit -> Actions tu chay (hoac Run workflow).

KY VONG LAN CHAY KE TIEP:
- Preflight XANH: "Compile Sources : 16 entry | file nguon tren dia : 20"
  + 4 dong "bo qua co chu dich" (AppDelegate + 3 tombstone) + [STEP END] exit=0
- Job di tiep: Select Xcode -> Build .app (xcodebuild that, log day du vao
  xbuild.log) -> Verify .app -> Package .ipa -> Verify .ipa
- Artifact: 'xbuild-log' (LUON co) + '<app>-trollstore-unsigned' (khi thanh cong)

TUY CHON (khong bat buoc): xoa han 3 file tombstone khoi repo (GitHub UI:
mo file -> thung rac -> Commit). Build khong doi — tombstone khong chua code.

TRUNG THUC:
- Sandbox khong co macOS/Xcode: xcodebuild/archive/IPA = NOT VERIFIED cho
  toi khi run ke tiep chay xanh. Neu build that bai, tai artifact 'xbuild-log'
  -> file xbuild.log gio chua TOAN BO output compiler + error dump, gui lai
  de phan tich tiep.
- Khong can bat ky Secret nao: pipeline unsigned TrollStore (CODE_SIGNING_
  ALLOWED=NO) la cau hinh SAN PHAM co y (IPA khong ky de cai qua TrollStore),
  khong phai thu thuat bao xanh — .ipa that su duoc dong goi va kiem tra
  (Mach-O arm64/arm64e, platform iOS, cau truc Payload/) truoc khi upload.

=== RUN #26 BI CHAN BILLING (job khong duoc cap runner) — CACH SUA ===
Loi KHONG nam trong code: "The job was not started because recent account
payments have failed or your spending limit needs to be increased."
Chon 1 trong 4 duong (khong duong nao sua code app):
 (A) Repo PUBLIC  -> phut Actions mien phi vo han:
     repo Settings -> General -> Danger Zone -> Change visibility -> Public.
 (B) SELF-HOSTED RUNNER tren Mac cua ban (co Xcode) -> khong ton phut GitHub:
     repo Settings -> Actions -> Runners -> New self-hosted runner, lay TOKEN
     (KHONG gui token cho ai/chat), tren Mac chay:
       mkdir actions-runner && cd actions-runner
       curl -L -o runner.tar.gz https://github.com/actions/runner/releases/latest/download/actions-runner-osx-arm64.tar.gz
       tar xzf runner.tar.gz
       ./config.sh --url https://github.com/Binkyo2013/BinTV1 --token <TOKEN>
       ./run.sh          # giu cua so terminal chay
     Workflow DA san cong tac: Actions -> Run workflow -> runner = self-hosted.
     (Mac can Xcode da cai + chap nhan license: sudo xcodebuild -license accept)
 (C) SUA BILLING: github.com/settings/billing -> Payment information (cap nhat
     the bi fail) + Spending limits -> nang han muc Actions; hoac cho reset
     chu ky (xem repo -> Actions -> Usage: phut da dung + ngay reset).
 (D) Nang goi Pro/Team de them phut kem theo.
Sau khi runner thong: Re-run jobs #26 hoac dispatch moi. NEU commit #26 chua
gom 3 file tombstone (chi sua yml), upload them 3 file tombstone trong zip
nay truoc khi re-run, keo Preflight bat lai loi 3 file cu.
